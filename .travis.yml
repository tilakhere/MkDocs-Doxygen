dist: trusty
sudo: required
language: cpp
  
addons:
  apt:
      update: true
      
addons:
  apt:
    packages:
      - doxygen
    
  sources:
        - ubuntu-toolchain-r-test
  packages:
        - doxygen

env:
    global:
    - VERBOSE=1
    
install: 
    - sh ci/.travis_install.sh

script: sh ci/.travis_test.sh 

